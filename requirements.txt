fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
biopython>=1.79
pyrodigal>=3.4

# Phage-specific gene caller -- finds substantially more real genes on
# phage genomes than general callers (Prodigal/Pyrodigal) because it's
# tuned for phages' dense, overlapping gene layout. Pyrodigal is kept
# above as an automatic fallback if this ever fails.
phanotate>=1.6.7

# BACPHLIP and its pinned classifier dependency — do not change the
# scikit-learn version, see the comment in Dockerfile for why.
bacphlip==0.9.6
scikit-learn==0.23.1

# Pinned deliberately: NumPy 1.24 removed the deprecated aliases (np.float,
# np.int, np.bool, etc.) that scikit-learn 0.23.1's internals still use.
# Anything >=1.24 breaks BACPHLIP's classifier at import time with
# "AttributeError: module 'numpy' has no attribute 'float'". Don't remove
# this pin without testing the exact scikit-learn/numpy pair together.
numpy==1.23.5

pandas>=1.0
joblib>=0.13
